<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.sas.question.model.dao.QuestionDao">
	<select id="totalCount" resultType="int">
		select count(*) from question
		<if test="questionWriterType != 0">
		 where question_writer_type = #{questionWriterType}
		 </if>
	</select>
	<select id="selectQuestionList" resultType="question">
		select * from
			(select rownum as rnum, q.* from(
			    select
			        question_no,
			        question_title,
			        question_type,
			        question_Writer
			    from question 
				<if test="questionWriterType != 0">
				 where question_writer_type = #{questionWriterType}
				 </if>	    
			     order by question_no desc)q)
			where rnum between #{pi.start} and #{pi.end}
	</select>
</mapper>
